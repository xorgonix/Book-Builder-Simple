# Book Builder local runtime config.
# Plain ASCII, visible, and safe to edit.

llm_mock=0
local_llm_url=http://localhost:1234/v1/chat/completions
local_llm_model=qwen/qwen3.5-9b

# Used only when a project does not have Metadata -> Author / Pen Name saved.
book_author=
book_language=en
